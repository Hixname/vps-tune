# Changelog

本项目采用 Semantic Versioning。

## [2.1.0] - 2026-08-03

### Added

- 档位菜单直接标注 1H1G/1H2G、至少 2G 内存和专项测试的适用场景；
- 生效检测增加实际 BDP 覆盖、CPU/内存、swap、默认网卡错误、qdisc 丢包和 NOFILE 摘要；
- 1 核千兆节点的 Xray 加密 CPU 瓶颈提示；
- 200/1000Mbps 与 175ms 的回归公式测试；
- 带单位的带宽和 RTT 输入测试。

### Changed

- RTT 菜单和环境变量可接受 `175`、`175ms` 或 `175 ms`；
- 带宽菜单和环境变量可接受 `1000`、`1000M` 或 `1000Mbps`；
- 允许在带宽/RTT 菜单的第一次提示中直接输入自定义数值；
- `NOFILE=65535` 在包装诊断中标记为可接受，不为相差 1 自动改写 systemd；
- README 增加按执行顺序编号的详细安装、参数、诊断、测速报告和恢复说明。

### Safety

- 保留现有 BDP 公式、8/16/32 MiB 档位下限和 64 MiB 绝对上限；
- 不根据一次外部测速自动修改套餐带宽或 RTT；
- 运行时诊断为只读检查，不会自动发起外部测速或改写妙妙屋X配置。

## [2.0.0] - 2026-08-03

### Added

- 编号主菜单：全新安装、覆盖重新安装、状态、恢复、生效检测；
- 保守、激进、极限三种可解释的 BDP 缓冲余量；
- 50/100/150/200/300 ms 和自定义 RTT 菜单；
- sysctl 冲突按真实路径去重，避免软链接重复报告；
- 经 `MIGRATE` 确认后完整备份原文件并只迁移受管键；
- 恢复时验证备份哈希和迁移后哈希，拒绝覆盖外部修改；
- 覆盖重装在保留原始证据的同时安全清理旧管理状态；
- 自动化公式与 64 MiB 边界测试。

### Changed

- 一键安装默认进入菜单；
- 所有档位使用显式、按 MiB 向上取整的 socket 缓冲上限；
- README 按实际执行顺序编号；
- 安装器与包装脚本版本升级至 `2.0.0`。

### Safety

- 三档只改变上游已支持的缓冲参数，不覆盖其 BBR/fq/backlog 基线；
- 极限档位需要输入 `EXTREME`；
- 覆盖重装需要输入 `REINSTALL`；
- 完整恢复需要输入 `RESTORE`。

### Fixed

- 将复合状态检查改为明确的 `if` 分支，兼容 GitHub Actions 的 ShellCheck 0.9.0。

## [1.1.0] - 2026-08-03

### Added

- 运行时带宽菜单：200、500、1000 Mbps 和自定义；
- 1001–10000 Mbps 扩展输入；
- 基于真实带宽与目标 RTT 的 16/32/64 MiB 缓冲选择；
- 超过 64 MiB 理论 BDP 时安全拒绝；
- GitHub 一键安装器、中文 README、安全说明和静态检查。
- 安装器默认绑定公开仓库 `Hixname/vps-tune`，仍允许环境变量覆盖以测试 fork。

### Safety

- 固定上游 `debian-vps-tuning` commit；
- 对四个上游资源脚本使用内置 SHA-256；
- 高于 1000 Mbps 时不修改上游代码，而是使用其兼容输入和显式缓冲；
- 不自动修改 UFW，不自动重启 VPS。

### Fixed

- 将范围检查改为明确的条件分支，兼容 GitHub Actions 使用的 ShellCheck，运行逻辑不变。

## [1.0.0] - 2026-08-03

### Added

- 自动识别 Debian 12/13 和 1C1G/1C2G 档位；
- 识别妙妙屋X systemd external/embedded 和 Docker host-network；
- 自动安装最小依赖；
- 上游下载与 SHA-256 校验；
- 预检、应用、验证、状态和回滚包装流程。
